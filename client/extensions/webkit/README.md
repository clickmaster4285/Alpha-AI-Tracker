# WebKit engine — not supported

WebKit-based browsers (Safari, GNOME Web / Epiphany, and similar) do not
expose the Chromium/Gecko WebExtensions + Native Messaging bridge that
Alpha AI Tracker uses for journey capture.

Detected WebKit browsers appear in the client UI as **Not supported** —
no install button, no native-host registration.
