# Live Stream WebSocket base URL (no path). Next.js rewrites do not proxy WS.
# Dev: point at the Go server directly. Prod: same host as the API via reverse proxy.
NEXT_PUBLIC_WS_URL=ws://localhost:8080
